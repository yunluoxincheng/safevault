/**
 * MVVM ViewModel layer.
 *
 * <p>Coordinates UI state and delegates data/security operations to the
 * backend abstraction instead of directly accessing persistence.
 */
package com.ttt.safevault.viewmodel;

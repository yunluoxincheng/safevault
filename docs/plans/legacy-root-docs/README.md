# Legacy Root Docs Archive

This folder keeps historical root-level documents that were moved out of repository root during documentation normalization.

These files are preserved for traceability but may contain outdated assumptions or encoding issues. Validate against current code and current docs before reusing details.
